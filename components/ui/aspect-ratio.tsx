"use client"

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// 定义一个名为 AspectRatio 的常量，其值为 AspectRatioPrimitive 库中的 Root 组件
const AspectRatio = AspectRatioPrimitive.Root

// 导出 AspectRatio 常量供其他模块使用
export { AspectRatio }